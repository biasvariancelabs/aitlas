{
    "model": {
        "classname": "aitlas.models.BaseClassifier",
        "config": {
            "num_classes": 10,
            "learning_rate": 0.001
        }
    },
    "task": {
        "classname": "aitlas.tasks.TrainTask",
        "config": {
            "num_epochs": 10,
            "model_directory": "./temp/experiment",
            "iterations_per_epoch": 200,
            "save_steps": 400
        }
    }
  }
