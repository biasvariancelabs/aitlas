version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - method: uv
      command: sync
      # For doc tools that are separated into a dependency group in pyproject.toml
      # groups:
      #   - docs

formats:
  - epub
